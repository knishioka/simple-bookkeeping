# Docker Compose Test Environment Variables
# Copy this file to .env.docker and update with your values

# Supabase Configuration (Required)
# These values must be set for Docker tests to run
# For test environments, you can use dummy values:
# - URL: https://dummy.supabase.co
# - ANON_KEY: any valid JWT token format
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Note: 
# - The ANON_KEY is a public key designed to be exposed in frontend code
# - For CI/CD, set these as environment secrets
# - For local testing, create a .env.docker file with your values
