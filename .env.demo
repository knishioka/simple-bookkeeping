# Demo Environment Configuration
# モック/デモ環境用の設定

# Port Configuration
WEB_PORT=3000
API_PORT=3001
NGINX_PORT=8080

# Database (temporary, no persistence)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=simple_bookkeeping

# API Configuration
NODE_ENV=demo
JWT_SECRET=demo-jwt-secret-for-testing-only
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=demo-refresh-secret-for-testing-only
JWT_REFRESH_EXPIRES_IN=7d

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Demo User Credentials (for documentation)
DEMO_EMAIL=demo@simple-bookkeeping.com
DEMO_PASSWORD=demo123