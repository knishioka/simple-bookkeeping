# env/secrets/ai.env
# Optional: automation / AI specific credentials with restricted scopes.

# Example (replace with your own scoped token):
# VERCEL_TOKEN_AI=vercel-token-with-preview-only-scope
# SUPABASE_ACCESS_TOKEN_AI=sbp_ai_agent_token_readonly

# OpenAI API Key (Optional)
# Used for CSV import AI-powered account classification
# If not set, CSV import will use rule-based classification instead
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...
