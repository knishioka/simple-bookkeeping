# env/secrets/common.env
# Shared defaults that are safe to store locally without exposing secrets.

# Node/Next defaults
NODE_ENV=development
LOG_LEVEL=info

# Local URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
WEB_PORT=3000
API_PORT=3001

# Optional toggles
ENABLE_SWAGGER=true
